- Fair Trip no longer needs MinimapAPI (thanks @Porp!): pathfinding is now standalone and the MinimapAPICompat switch is retired. FairTripTime is OFF by default — turn it on in MCM if you want fair time penalties; players who had opted in via the old switch keep their setting
- New: custom overlay key (MCM: OverlayKey / OverlayKeyController, thanks @Porp!) — bind a different key/button to open the teleport map, e.g. to dodge EID's TAB item reminder; unbound = vanilla map key, bound = replaces it
- Fixed a room-adjacency wrap-around inherited from the original GoodTrip: a room on the map's left edge could treat a room on the right edge of the neighboring row as its "cleared neighbor", allowing a teleport that should have been rejected (also affected fair trip's last-hop check)
